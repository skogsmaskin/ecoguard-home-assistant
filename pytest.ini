[pytest]
minversion = 7.4
addopts = 
    --strict-markers
    --strict-config
    --disable-warnings
    -ra
testpaths = tests
asyncio_mode = auto
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
